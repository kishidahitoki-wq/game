name: CI Workflow

# どのタイミングで実行するか（ここではメインブランチへのプッシュ時）
on:
  push:
    branches: [ "feature/try-github-actions" ]
  pull_request:

jobs:
  test:
    # 実行するOS環境
    runs-on: ubuntu-latest

    steps:
      - name: PR出された側
        run: echo "This is the feature/try-github-actions branch"